[kratos]
addr = "http://localhost:4434"

[service]
addr = "0.0.0.0"
ports.http = "8080"
ports.http_health = "1040"
ports.grpc = "5050"
ports.grpc_health="1030"

[tls]
certificate = "certificate/server_certs/server.cert.pem"
key = "certificate/server_certs/server.key.pem"
cert_autority = "certificate/certs/cacert.pem"
